@{
    Layout = "~/Views/Shared/_Index.cshtml";
}
<blockquote class="layui-elem-quote news_search">
    <div class="layui-inline">
        <form id="form" class="layui-form">
            <div class="layui-input-inline">
                <input type="text" id="keyword" placeholder="请输入关键字" class="layui-input search_input">
            </div>
            <a class="layui-btn search_btn layui-btn-small" id="btnSearch">查询</a>
        </form>
    </div>
    <div id="toolbar" class="layui-inline">
        <div id="layui-btn-group" class="layui-inline toolsTable"></div>
    </div>
</blockquote>
<div class="layui-form">
    <table id="gridList" lay-filter="gridList"></table>
</div>
@section scripts {
    <script type="text/javascript">
        var table = layui.table;
        initGrid();
        $("#btnSearch").click(initGrid);
        $("#toolbar").authorizeButton(window.location.pathname);

        function initGrid() {
            var _curr = 0;
            var _count = 0;
            var _keyword = $("#keyword").val();

            table.render({
                elem: '#gridList',
                id: 'gridList',
                url: '/Itsm/OrderTemplete/GetJsonData',
                cols: [[
                 { checkbox: true, fixed: true } 
                 , { field: 'Name', title: '简要描述' }
                 , { field: 'EffectName', title: '影响范围', sort: true }
                 , { field: 'UrgencyName', title: '紧急度', sort: true }
                 , { field: 'Description', title: '详细', sort: true }
                 , { field: 'IsEnable', title: '状态', width: 80, templet: '#IsEnableTpl' }
                ]],
                curr: _curr,
                where: { keyword: _keyword }
            });
        }

        function btn_add() {
            $.buttonAdd({
                title: "创建工单模版",
                content: "/Itsm/OrderTemplete/Form"
            });
        }
        function btn_edit() {
            $.buttonEdit({
                title: "修改工单模版",
                content: "/Itsm/OrderTemplete/Form"
            });
        }

        function btn_delete() {
            $.buttonChk({
                url: "/Itsm/OrderTemplete/Delete"
            });
        }
    </script>
}

